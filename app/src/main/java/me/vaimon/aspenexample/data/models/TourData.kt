package me.vaimon.aspenexample.data.models

import androidx.annotation.DrawableRes

data class TourData(
    val title: String,
    val imageUri: String,
    val nightsCount: Int,
    val daysCount: Int
)
